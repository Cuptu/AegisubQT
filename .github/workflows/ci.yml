name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-24.04, macos-14]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.11.2"
          modules: qtshadertools qtmultimedia qtwayland
          aqtsource: "git+https://github.com/miurahr/aqtinstall.git"


      - name: Setup build tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build

      - name: Setup build tools (macOS)
        if: runner.os == 'macOS'
        run: brew install ninja

      - name: Setup MSVC toolchain (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup Ninja (Windows)
        if: runner.os == 'Windows'
        run: choco install ninja

      - name: Configure
        run: cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="${{ env.Qt6_DIR }}"

      - name: Build
        run: cmake --build build --parallel

      # Headless smoke test: the app must stay alive (timeout exit code 124)
      # after loading QML; a startup failure exits non-zero before the timeout.
      - name: Smoke test (Linux, offscreen)
        if: runner.os == 'Linux'
        run: |
          export QT_QPA_PLATFORM=offscreen
          export QT_QUICK_BACKEND=software
          set +e
          timeout 20s ./build/AegisubQT > smoke.log 2>&1
          code=$?
          set -e
          cat smoke.log
          if [ "$code" -ne 0 ] && [ "$code" -ne 124 ]; then
            echo "::error::Smoke test failed with exit code $code"
            exit "$code"
          fi

      # Bundle Qt runtime DLLs next to the exe so the artifact runs out of the box.
      - name: Deploy Qt runtime (Windows)
        if: runner.os == 'Windows'
        run: windeployqt --release --compiler-runtime --qmldir qml build/AegisubQT.exe

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: AegisubQT-windows-x64
          path: |
            build/AegisubQT.exe
            build/*.dll
            build/platforms/
            build/imageformats/
            build/iconengines/
            build/generic/
            build/tls/
            build/multimedia/
            build/networkinformation/
            build/translations/
            build/qmltooling/
            build/qml/
            build/assets/
            build/automation/
            build/locale/
          if-no-files-found: error

      # Stage resources, deploy Qt runtime, ad-hoc sign, and verify bundle on macOS.
      - name: Deploy Qt runtime and verify bundle (macOS)
        if: runner.os == 'macOS'
        run: |
          mkdir -p build/AegisubQT.app/Contents/Resources
          cp -R qml assets automation build/AegisubQT.app/Contents/Resources/
          cp assets/icons_native/*.icns build/AegisubQT.app/Contents/Resources/
          mkdir -p build/AegisubQT.app/Contents/Resources/locale
          cp build/*.qm build/AegisubQT.app/Contents/Resources/locale/
          macdeployqt build/AegisubQT.app -qmldir="$GITHUB_WORKSPACE/qml" -verbose=1
          xattr -cr build/AegisubQT.app
          chmod 755 build/AegisubQT.app/Contents/MacOS/AegisubQT
          find build/AegisubQT.app/Contents/Frameworks -type f -name "*.dylib" -exec codesign --force --timestamp=none --sign - {} \;
          for fw in build/AegisubQT.app/Contents/Frameworks/*.framework; do
            if [ -d "$fw" ]; then
              codesign --force --timestamp=none --sign - "$fw"
            fi
          done
          find build/AegisubQT.app/Contents/PlugIns -type f -name "*.dylib" -exec codesign --force --timestamp=none --sign - {} \;
          codesign --force --timestamp=none --sign - --entitlements packages/osx/aegisub.entitlements build/AegisubQT.app/Contents/MacOS/AegisubQT
          codesign --force --timestamp=none --sign - build/AegisubQT.app
          codesign --verify --deep --strict --verbose=2 build/AegisubQT.app

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: AegisubQT-macos-arm64
          path: build/AegisubQT.app
          if-no-files-found: error

      - name: Upload artifact (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: AegisubQT-linux-x64
          path: |
            build/AegisubQT
            build/qml/
            build/assets/
            build/automation/
            build/locale/
          if-no-files-found: error
